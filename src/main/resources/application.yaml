spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/salary_income
    username: root
    password: ${db_password}

  jackson:
    parser:
      allow-unquoted-field-names: true
